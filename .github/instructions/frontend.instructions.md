# Frontend Instructions

- Use consistent component structure and naming.
- Keep UI accessible (labels, roles, keyboard navigation).
- Avoid unnecessary re-renders and duplicated state.
- Include or update UI tests when behavior changes.